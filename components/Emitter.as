package components {
	import starling.display.Sprite;
	
	public class Emitter extends Sprite {
		
		public function Emitter () {
			/* Ok, I know this needs properties. I'm thinking it'll take a count, and have a start function and a stop function.
			I think it'll be possible eventually to add things like variation in size, gradual change in color, etc...
			although I'm not sure how that'll perform, so let's stick with simpler things
			
			This needs a list of particles. Vector, probably.
			This needs a list of forces between each particle
			This needs 
			*/
		}
	}
}